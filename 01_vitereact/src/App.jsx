import Chai from './chai.jsx'

// function App() {
//   return (
//    <h1>Hello World</h1> //instead of returning a single element, we can return multiple elements by wrapping them in a parent element like a div or a fragment. This allows us to create more complex UI structures and organize our components better.
//   )
// }

function App() {
  return (
   <Chai /> //instead of returning a html element, we can return a React component like Chai. This allows us to create reusable components and build more complex UIs by composing smaller components together.
  )
}

export default App
